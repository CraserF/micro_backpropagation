{
  "name": "llm-api-service",
  "version": "0.0.0",
  "private": true,
  "description": "Client-side-friendly LLM API gateway (Firebase-style auth + DeepSeek inference)",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=20"
  }
}
